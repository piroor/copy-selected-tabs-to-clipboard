{
  "name": "copy-selected-tabs-to-clipboard",
  "version": "0.0.0",
  "engines": {
    "node": ">=8.6.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "babel-core": "^6.0.0",
    "babel-plugin-module-resolver": "^3.0.0",
    "eslint": "^5.0.1",
    "eslint-import-resolver-babel-module": "^4.0.0",
    "eslint-plugin-import": "*",
    "jsonlint-cli": "*",
    "tunnel-agent": ">=0.6.0"
  }
}
